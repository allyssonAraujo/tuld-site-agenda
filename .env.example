# Database PostgreSQL
# Render vai inserir isto automaticamente como DATABASE_URL
DATABASE_URL=postgresql://user:password@localhost:5432/tuld

# Node Environment
NODE_ENV=development

# Session Secret (gere um valor aleatório para produção)
SESSION_SECRET=sua_chave_secreta_super_segura_aqui_2026

# Port (Render vai overridecomport via variável PORT)
PORT=3000
